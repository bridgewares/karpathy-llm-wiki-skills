# 命名规范

## 通用规则
- 优先使用清晰、稳定、可读的中文标题。
- 若 vault 中已有命名方式，优先延续现有风格。
- 文件名避免频繁改动，保证链接稳定。

## 时间格式
- 展示型日期可使用 `YYYY-MM-DD`。
- 所有用于状态判断的时间字段必须使用 `YYYY-MM-DD HH:MM:SS`，精确到秒。

## 路径约定
- 新来源先放 `raw/inbox/<type>/`。
- 正式来源放 `raw/library/<type>/`。
- 来源页放 `wiki/sources/<type>/`。

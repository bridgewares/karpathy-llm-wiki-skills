# 现有维护偏好

这个 vault 已经在使用中，初始化时只应补齐缺失结构，不应覆盖我已有的协作规则。

## 已有偏好
- 保留我已有的写作习惯
- 仅补充缺失的 wiki 规则，不要整篇重写
- 如果发现我已经写了 vault 专属 CLAUDE.md，优先在其基础上增补，而不是替换成默认版本
- 不要把这里已有的偏好改成另一套通用规范

## 当前状态
- 这个 vault 还缺一些 wiki 骨架文件
- 但现有协作约束已经在使用

# 工作流

## ingest
1. 新来源先进入 `raw/inbox/`。
2. 确认后移动到 `raw/library/`。
3. 更新 `raw-index.md`。
4. 在 `wiki/` 中创建或更新对应页面。

## query
1. 先看 `index.md`。
2. 再看相关 `wiki/` 页面。
3. 不足时再回到 `raw/`。

## lint
1. 检查 `raw/library/` 与 `raw-index.md` 是否一致。
2. 检查来源页与知识页是否存在明显断链或陈旧状态。

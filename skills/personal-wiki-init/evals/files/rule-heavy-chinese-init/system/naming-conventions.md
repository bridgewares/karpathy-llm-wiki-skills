# 命名规范

## 总体原则
- 中文优先，保留必要英文术语。
- 文件名保持稳定、清晰、可读。
- 不为链接而链接，避免噪音。

## 目录命名
- `raw/` 只用于原始资料。
- `wiki/` 只用于整理后的知识页。
- `raw/inbox/` 与 `raw/library/` 必须分开。

## 页面命名
- 来源页名称应与来源标题或稳定标识保持一致。
- 主题、概念、人物、问题页优先使用清晰直白的中文标题。

## 时间格式
- 展示型日期可使用 `YYYY-MM-DD`。
- 所有用于状态判断的时间字段必须精确到秒，统一使用 `YYYY-MM-DD HH:MM:SS`。

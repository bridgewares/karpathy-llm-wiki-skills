# 命名规范

## 文件与标题
- 优先使用清晰直白的中文标题。
- 文件名保持稳定、可读，避免频繁改名。
- 不为链接而链接，关键主题、概念、人物再建立 wiki 链接。

## 时间格式
- 展示型日期可使用 `YYYY-MM-DD`。
- 所有用于状态判断的时间字段必须精确到秒，统一使用 `YYYY-MM-DD HH:MM:SS`。

## 路径约定
- 新来源先进入 `raw/inbox/<type>/`。
- 正式来源存放在 `raw/library/<type>/`。
- 只有 `raw/library/` 中的来源进入 `raw-index.md`。
